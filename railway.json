{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn app:app --bind 0.0.0.0:5000 --workers 4 --timeout 120",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100
  }
}
